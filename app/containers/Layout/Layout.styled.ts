import styled from 'styled-components';

export const StyledLayout = styled.div`
  /* overflow: hidden;
  height: 100vh;
  display: grid;
  grid-template-rows: auto 1fr;
  main {
    overflow-y: scroll;
    padding: 0 10vw;
  } */
`;
